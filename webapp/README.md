# Address Search WebApi

## Development Setup

### Secrets

Initialize secrets with:
`dotnet user-secrets init`

add secrets for azure search setup:
`dotnet user-secrets set "SearchClient:Credential:Key" "<your-api-key>"`
